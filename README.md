# ssebsShare

WebRTC Screen Sharing

## Usage:
- Set `client/src/App.js` `io.connect` line to your server name, or localhost
- ```bash
  $ npm install
  $ cd client
  $ npm install
  $ npm run build
  $ cd ..
  $ npm run start
  ```
## LICENSE
[MIT](./LICENSE)
